{
  "require-dev": {
    "phpstan/phpstan": "^1.0",
    "php-stubs/wordpress-stubs": "^6.0",
    "szepeviktor/phpstan-wordpress": "^1.3",
    "rector/rector": "^1.2",
    "kint-php/kint": "^5.0",
    "php-stubs/acf-pro-stubs": "^6.5"
  },
  "scripts": {
    "analyse": "vendor/bin/phpstan analyse > storage/logs/phpstan/phpstan.log || true",
    "rector": "vendor/bin/rector process --dry-run > storage/logs/rector/rector.log || true",
    "rector-fix": "vendor/bin/rector process > storage/logs/rector/rector-fix.log || true"
  },
  "autoload": {
    "psr-4": {
      "App\\": "app/"
    },
    "files": [
      "bootstrap/functions.php"
    ]
  },
  "require": {
    "monolog/monolog": "^3.0"
  },
  "config": {
    "platform": {
      "php": "8.2.0"
    }
  }
}
